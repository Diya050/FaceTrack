import { Box, Typography } from "@mui/material";


const BulkUpload = ({ }) => {
  return (
    <Box p={2}>
      <Typography variant="h6">
        { "Bulk Upload" }
      </Typography>
    </Box>
  );
};

export default BulkUpload;