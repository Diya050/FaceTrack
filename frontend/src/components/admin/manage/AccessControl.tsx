import { Box, Typography } from "@mui/material";


const AccessControl = ({ }) => {
  return (
    <Box p={2}>
      <Typography variant="h6">
        { "Access Control" }
      </Typography>
    </Box>
  );
};

export default AccessControl;