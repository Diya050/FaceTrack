import { Box, Typography } from "@mui/material";


const UserProfiles = ({ }) => {
  return (
    <Box p={2}>
      <Typography variant="h6">
        { "User Profiles" }
      </Typography>
    </Box>
  );
};

export default UserProfiles;