import { Box, Typography } from "@mui/material";


const Roles = ({ }) => {
  return (
    <Box p={2}>
      <Typography variant="h6">
        { "Roles" }
      </Typography>
    </Box>
  );
};

export default Roles;