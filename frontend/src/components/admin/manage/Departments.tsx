import { Box, Typography } from "@mui/material";


const Departments = ({ }) => {
  return (
    <Box p={2}>
      <Typography variant="h6">
        { "Departments" }
      </Typography>
    </Box>
  );
};

export default Departments;