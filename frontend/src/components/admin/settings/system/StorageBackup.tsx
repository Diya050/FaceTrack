import { Box, Typography } from "@mui/material";


const StorageBackup = ({ }) => {
  return (
    <Box p={2}>
      <Typography variant="h6">
        { "Storage & Backup" }
      </Typography>
    </Box>
  );
};

export default StorageBackup;