import { Box, Typography } from "@mui/material";


const SystemStatus = ({ }) => {
  return (
    <Box p={2}>
      <Typography variant="h6">
        { "System Status" }
      </Typography>
    </Box>
  );
};

export default SystemStatus;