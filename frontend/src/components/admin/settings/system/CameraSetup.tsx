import { Box, Typography } from "@mui/material";


const CameraSetup = ({ }) => {
  return (
    <Box p={2}>
      <Typography variant="h6">
        { "Camera Setup" }
      </Typography>
    </Box>
  );
};

export default CameraSetup;