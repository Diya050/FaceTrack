import { Box, Typography } from "@mui/material";


const AccessLogs = ({ }) => {
  return (
    <Box p={2}>
      <Typography variant="h6">
        { "Access Logs" }
      </Typography>
    </Box>
  );
};

export default AccessLogs;