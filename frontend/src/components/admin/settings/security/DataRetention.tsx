import { Box, Typography } from "@mui/material";


const DataRetention = ({ }) => {
  return (
    <Box p={2}>
      <Typography variant="h6">
        { "Data Retention" }
      </Typography>
    </Box>
  );
};

export default DataRetention;