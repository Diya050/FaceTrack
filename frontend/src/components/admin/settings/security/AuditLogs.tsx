import { Box, Typography } from "@mui/material";


const AuditLogs = ({ }) => {
  return (
    <Box p={2}>
      <Typography variant="h6">
        { "Audit Logs" }
      </Typography>
    </Box>
  );
};

export default AuditLogs;