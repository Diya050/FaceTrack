import { Box, Typography } from "@mui/material";


const UserGuide = ({ }) => {
  return (
    <Box p={2}>
      <Typography variant="h6">
        { "User Guide" }
      </Typography>
    </Box>
  );
};

export default UserGuide;