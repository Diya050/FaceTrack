import { Box, Typography } from "@mui/material";


const AdminGuide = ({ }) => {
  return (
    <Box p={2}>
      <Typography variant="h6">
        { "Admin Guide" }
      </Typography>
    </Box>
  );
};

export default AdminGuide;