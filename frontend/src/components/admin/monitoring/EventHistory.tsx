import { Box, Typography } from "@mui/material";


const EventHistory = ({ }) => {
  return (
    <Box p={2}>
      <Typography variant="h6">
        { "Event History" }
      </Typography>
    </Box>
  );
};

export default EventHistory;