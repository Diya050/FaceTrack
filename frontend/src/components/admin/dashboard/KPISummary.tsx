import { Box, Typography } from "@mui/material";


const KPISummary = ({ }) => {
  return (
    <Box p={2}>
      <Typography variant="h6">
        { "KPI Summary" }
      </Typography>
    </Box>
  );
};

export default KPISummary;