import { Box, Typography } from "@mui/material";


const SystemHealth = ({ }) => {
  return (
    <Box p={2}>
      <Typography variant="h6">
        { "System Health" }
      </Typography>
    </Box>
  );
};

export default SystemHealth;