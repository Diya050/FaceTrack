import { Box, Typography } from "@mui/material";


const Overview = ({ }) => {
  return (
    <Box p={2}>
      <Typography variant="h6">
        { "Overview" }
      </Typography>
    </Box>
  );
};

export default Overview;