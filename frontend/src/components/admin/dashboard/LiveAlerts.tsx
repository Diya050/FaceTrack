import { Box, Typography } from "@mui/material";


const LiveAlerts = ({ }) => {
  return (
    <Box p={2}>
      <Typography variant="h6">
        { "Component Title" }
      </Typography>
    </Box>
  );
};

export default LiveAlerts;